# 日本株データ・解析まわりのメモ

このリポジトリ自体はシンプルなTimerアプリ(`index.html`のみ)だが、
このセッション(Claude Code)を使って、別件として日本株の日足データを使った
テクニカル指標バックテスト・分析作業を行っている。今後も同じ作業を頻繁に
依頼される想定なので、Google Drive上のデータ構成をここに記録しておく。

## Google Driveのフォルダ構成

親フォルダ: `https://drive.google.com/drive/folders/1VbAPZHg-zOcC4JXYh6ZsRvgiY5h_8naY`
(ID: `1VbAPZHg-zOcC4JXYh6ZsRvgiY5h_8naY`)

直下に4つのフォルダがある。

| フォルダ名 | フォルダID | 内容 |
|---|---|---|
| 株価四本値 | `1qHd_3p8NPmPRDVcElr9QjOwS3Dm_A5Go` | 元データ。銘柄ごとに1ファイル(約4,668ファイル、約1GB)。ETF/REIT等も混在 |
| **統合CSV(全銘柄)** | `1HF6A9VcrW3ZSPW9OD7G7k6ZOnpqk4EuJ` | **上記を47ファイルに統合したもの。今後の分析はここを使う(下記参照)** |
| 解析_テクニカル指標検証 | `1wdFw7PK4htqREyAkLTctF9nLk_jCFuXz` | 過去の分析出力(取引ログCSV等) |
| バックテスト結果_2026-09-16 | `1T7sFN54cfyGHKV4FRmkuh83NWPF7keci` | バックテストのレポート(Googleドキュメント/スプレッドシート、600銘柄サンプル版・全銘柄版) |

## 「株価四本値」フォルダ(元データ)の仕様

- ファイル名: `{証券コード}_{銘柄名}.csv` (例: `79850_ネポン.csv`)
- CSV列: `Date,Code,O,H,L,C,UL,LL,Vo,Va,AdjFactor,AdjO,AdjH,AdjL,AdjC,AdjVo,MktCap,ExRT`
  - `AdjO,AdjH,AdjL,AdjC` が株式分割調整後の始値・高値・安値・終値。**分析にはこちらを使うこと**(未調整の`O,H,L,C`ではない)
  - データ期間はおおむね2016年頃〜直近
- 欠損行が多い銘柄(上場廃止・整理銘柄等)があるので、有効データ比率のチェックが必要な場合がある

## 「統合CSV(全銘柄)」フォルダ(★分析にはこちらを優先して使う)

個別銘柄ファイルを1つずつDriveから取得すると、Claude Codeのトークンを大量に消費する
(数千回のダウンロード呼び出しが発生するため)。これを避けるため、Google Apps Script
(ユーザーのDriveアカウント上で実行、Claude経由ではない)で以下のように統合済み。

- ETF・REIT・投資信託等をファイル名キーワードで除外した**3,889銘柄**が対象
  (除外キーワード: ＥＴＦ, ETF, 上場投信, 投資法人, リート, REIT, ファンド, インデックス,
  連動型, アセットマネジメント, 資産運用, ＥＴＮ, 上場投資信託, セキュリティーズ, トラスト, 投信)
- 証券コード順に100銘柄ずつ、`merged_0001.csv` 〜 `merged_0047.csv` の**47ファイル**にまとめてある
  (各ファイルは元データと同じ列構成のCSVをそのまま連結したもの。ヘッダー行は先頭のみ)
- `index.csv` — 「どの証券コードがどの`merged_XXXX.csv`に入っているか」の対応表
  (列: `code,name,chunk_file`)
- `_manifest.json` — 統合処理の内部作業用ファイル(分析には不要)

### 使い方(トークン節約のコツ)

1. まず`index.csv`をダウンロードして、必要な銘柄コードがどの`merged_XXXX.csv`に
   入っているか調べる
2. 全銘柄が必要な場合でも、47ファイルだけ取得すればよい(個別4,668ファイルより遥かに少ない)
3. 一部の銘柄だけ必要な場合は、該当する`merged_XXXX.csv`だけを取得すればよい
4. 各`download_file_content`の結果は大きい場合ハーネスが自動でローカルファイルに退避してくれる
   (トークン消費は最小限の通知メッセージのみ)ので、47ファイル程度なら気にせず並列取得してよい

### 再生成が必要になった場合

「株価四本値」フォルダが更新された場合など、統合CSVを作り直す必要が出たら、
Google Apps Script側で再実行が必要(Claude Code側からはDrive上のファイル書き込みが
大量になるとトークンを消費するため、直接の再構築はしない方針)。スクリプトの実装は
このセッションの会話履歴を参照。

## 過去の作業の経緯(参考)

- 49銘柄でのチャット版Claude試験検証 → 600銘柄サンプルでの検証 → 全銘柄(3,541銘柄)での
  最終検証、という順で段階的にバックテストを実施済み
- 検証内容: ゴールデンクロス・RSI・MACD・ボリンジャーバンド・52週高値ブレイクの
  5指標について、統一の売買ルールでバックテストし勝率・平均リターンを比較
- 結果はすべて「バックテスト結果_2026-09-16」フォルダのドキュメント・スプレッドシートに記録済み
  (RSI・ボリンジャーバンドが他指標より高い勝率・リターンを示す傾向が、銘柄数を
  拡大しても一貫して確認された)
